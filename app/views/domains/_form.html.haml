= form_for @domain do |f|
  .clearfix
    = f.label :name
    .input
      = f.text_field :name
  .clearfix
    = f.label :url
    .input
      = f.text_field :url
  .actions
    = f.submit "Save", :class => "btn primary"
    = link_to "Cancel", :back, :class => "btn"